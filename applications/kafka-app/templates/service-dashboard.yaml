apiVersion: v1
kind: Service
metadata:
  name: kafka-dashboard-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: kafka-dashboard
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP